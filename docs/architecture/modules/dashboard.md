# dashboard

Web dashboard served alongside the API. Phase 1 (largely complete): stats and observability. Phase 2 (future): interaction. Phase 3 (future): configuration UI.

**Paths:** see [`.changes/catalogs/modules.toml`](../../../.changes/catalogs/modules.toml).

**Related specs:** none yet — see `CLAUDE.md` "Web Dashboard" section.
