# engine_api

HTTP API and MCP server — Axum routing, handlers, OpenAPI schema generation via utoipa, middleware (auth, tracing), and HTTP error surfaces. Handlers are thin: extract → service call → format response.

**Paths:** see [`.changes/catalogs/modules.toml`](../../../.changes/catalogs/modules.toml).

**Related specs:** [spec/08-api.md](../../../spec/08-api.md).
