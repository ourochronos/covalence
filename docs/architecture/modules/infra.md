# infra

Build, deploy, sidecar containers, and top-level project metadata: Makefile targets, docker-compose, deployment scripts, sidecar service definitions (PDF converter, fastcoref), and the example configuration files.

**Paths:** see [`.changes/catalogs/modules.toml`](../../../.changes/catalogs/modules.toml).

**Related specs:** none — operational concerns documented inline in the Makefile and per-script README files.
