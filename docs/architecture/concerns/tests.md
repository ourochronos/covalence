# tests

Unit tests, integration tests (gated by `--ignored`), evaluation harness coverage. For each affected module: what new tests are added, are integration tests warranted, does the eval harness need updated fixtures.

INV-8 forbids synthetic test data — fixtures are real data or clearly marked.
