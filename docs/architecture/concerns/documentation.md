# documentation

User-facing guides, inline rationale (sparingly — see CLAUDE.md), CLAUDE.md updates if conventions change, ADR if the decision is architectural.

Every architectural change updates at minimum its own `decisions.md`. Most also touch the spec it anchors to and may propose ADR promotion.
