# cli_ux

`cove` CLI command behavior, output formatting (`--json` flag), and error messages. Applies even to engine changes when the change exposes a new capability that the CLI should surface.

CLI is the primary interaction surface (CLAUDE.md "CLI-first interaction"). Missing commands are signal — add them rather than work around them.
